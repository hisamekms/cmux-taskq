# dagq が run に渡す設定。runtime が読むのは main checkout の作業ファイルで、
# run の worktree で変えても着地して main checkout に反映されるまで効かない。
# 書式と渡し先は docs/design/supervisor-lifecycle/run-environment.md。

[run.env]
# 依存 crate の compile の結果を sccache で run 間に共有する（ADR-0049 の決定 6・7）。
# target は run ごとの worktree の target/ のまま。CARGO_TARGET_DIR を run ごとの値にすると
# sccache の鍵に入って当たらなくなり、共有すると test と coverage の関門が誤るので置かない。
# CARGO_INCREMENTAL も変えない（切っても run 間の hit は増えず、worker の繰り返しの build が遅くなる）。
# sccache は人が `mise use -g sccache` で入れ、~/.local/bin/sccache に mise の shim への link を置く。
# 見つからなければ up・claim・integrate が止まり、inbox に install tool が出る（決定 8・9）。
RUSTC_WRAPPER = "sccache"
# server と話せないときは rustc を直接実行し、cache が効かないだけで build を失敗させない。
SCCACHE_IGNORE_SERVER_IO_ERROR = "1"
# run ごとの cargo の並列度を絞る（2026-09-26 に人が planner と決めた。task 427）。
# host は 8 コア / 16GB で、並列 4 の運用で load average が最大 151〜204 に達し、cmux の capture の
# timeout が 400 件を超え、run の startup の中央値が約 1100 秒になった（goal 36 の note 8718）。
# 値の決め方: supervisor の --parallel を 3 に下げ、worker 3 本と integrate 1 本が同時に cargo を回しても
# 合計 16 並列（コア数 8 の 2 倍）程度に収まるようにする。--parallel や host を変えたら合わせて見直す。
# CARGO_BUILD_JOBS は build の job 数で、この値で絞る。
CARGO_BUILD_JOBS = "4"
# test の並列度は build と分けて考え、8 にする（2026-09-26 に人が planner と決めた。task 566）。
# test は待ちが中心で CPU をあまり使わない（task 537 の測定で test の process 4 本の CPU は 0.1〜0.4 コア、
# 関門の test 段は per-test の時間の合計 ÷ 並列数で律速する。docs/plans/nextest-measurement.md）ので、
# 並列度を上げても host の負荷は小さく、integrate の test 段が縮む。
# RUST_TEST_THREADS は 1 つの test binary の中の並列 thread 数。
RUST_TEST_THREADS = "8"
# coverage の関門（cargo llvm-cov nextest）で同時に走らせる test の process 数（ADR-0076 の決定 2）。
# nextest は RUST_TEST_THREADS を読まず既定で CPU 数だけ走らせるので、RUST_TEST_THREADS と同じ値にする。
# .config/nextest.toml には書かない（CI と人の手元の nextest まで絞られるため）。
NEXTEST_TEST_THREADS = "8"
